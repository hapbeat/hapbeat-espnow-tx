# CLAUDE.md — hapbeat-transmitter-firmware

## repo の目的

ESP-NOW による送信専用ファームウェアを実装する repo。Bridge から受けた軽量コマンドを ESP-NOW パケットに変換し、Hapbeat 本体群へ無線配信する。

## 全体アーキテクチャ上の役割

Hapbeat SDK ワークスペース全体の中で、送信機ファームは「最終ホップ」を担う。

1. ホストアプリケーションが SDK を通じてコマンドを発行する
2. Bridge（host-bridge）がホスト側の制御面としてコマンドを受け取り、処理する
3. **送信機ファーム（本 repo）が Bridge から受けた軽量コマンドを ESP-NOW 無線送信に変換し、Hapbeat 本体へ配る**
4. Hapbeat 本体（device-firmware）がコマンドを受信し、触覚再生を行う

送信機ファームは Bridge と Hapbeat 本体の間に位置する中継専用レイヤーである。

## この repo の責務

- host-bridge からのコマンド受信（内部インターフェース経由）
- ESP-NOW パケットへの変換と送信
- Broadcast 送信（全デバイスへの一斉配信）
- Group send（特定グループへの送信）
- Seq（シーケンス番号）の付与と管理
- Retry（再送制御）の基本処理
- Delivery policy（送信保証レベル）の適用
- 必要に応じた送信ログ・送信状態管理

## 管理対象

- 送信機ファームウェアソースコード
- ESP-NOW 送信ロジック
- host-bridge 受信ハンドラ

## 管理対象外

以下は本 repo では扱わない。それぞれ担当の repo が存在する。

- Pack 形式（定義・解析・読み込み）
- wav / clip 管理・再生
- UI asset
- ローカル再生（デバイス上での触覚再生処理）
- Unity / Unreal / TouchDesigner / Max 向け public API
- デバイス本体の battery 管理 / UI / install 処理

## 依存関係

### 依存してよい repo

- **hapbeat-contracts** — 共通の型定義・コマンド定義・プロトコル定義を参照する

### 依存される repo

- なし。SDK からは直接参照されない。Bridge と内部インターフェースで接続される。

## 壊してはいけない公開インターフェース

- **Bridge との内部メッセージ互換** — Bridge が送信機ファームに渡すコマンドフォーマットの後方互換性
- **ESP-NOW 送信の基本保証** — パケットが正しく送信されること、seq が正しく付与されること
- **Group / broadcast の期待挙動** — broadcast は全デバイスに届くこと、group send は指定グループにのみ届くこと

## 他 repo に影響する変更の例

- **ESP-NOW パケットフォーマット変更** — hapbeat-device-firmware（本体ファーム）の受信処理に影響する。contracts で定義を共有し、両方を同期的に更新する必要がある
- **Bridge との内部プロトコル変更** — host-bridge 側の送信処理に影響する。内部インターフェースの変更は Bridge と合わせて行う

## やってはいけないこと

- 本体ファーム（device-firmware）の責務を取り込む（ローカル再生、Pack 処理、UI など）
- wav や Pack を処理し始める
- SDK 直結の公開 API をここで定義する（公開 API は SDK 側の責務）
- Bridge の上位ロジック（コマンド解釈・スケジューリングなど）を抱え込む

## まだ作らないもの

- 高度な再送制御（ACK ベースの信頼性保証など）
- 暗号化（ESP-NOW レベルの暗号化対応）
- OTA（Over-The-Air アップデート機構）

これらは基本的な送信機能が安定した後に段階的に追加する。

## 今後の最初の着手タスク

1. **内部インターフェース境界の整理** — Bridge から送信機ファームへ渡すコマンド形式と通信手段を定義する
2. **送信パケット責務の整理** — ESP-NOW パケットに含めるべき情報（コマンド種別、seq、グループ ID など）を整理する
3. **Broadcast / group send / retry の基本方針整理** — 送信モードごとの挙動と再送ポリシーを決める
4. **Bridge から見た transmitter の状態モデル整理** — 送信機の接続状態・送信状態を Bridge がどう把握するかを定義する

## 実装の優先順位

まずはシンプルな送信責務に限定する。Bridge からコマンドを受け取り、ESP-NOW で送信するという最小限の流れを確立することが最優先。複雑な再送制御、高度なグループ管理、暗号化などは後から段階的に追加する。

## テストや確認の考え方

- **Seq の取り扱い** — シーケンス番号が正しく採番されるか、重複や欠番が発生しないか
- **重複送信の扱い** — 同一コマンドが複数回送信された場合の挙動（冪等性の確認）
- **Group send の整合性** — 指定グループに属するデバイスにのみ送信されるか、漏れや誤配信がないか
- **送信遅延** — Bridge からコマンドを受信してから ESP-NOW 送信完了までのレイテンシ
- **Delivery policy の動作確認** — 各送信保証レベルが期待どおりに機能するか

## オフライン動作をどう意識するか

送信機ファームはローカルネットワーク内で Bridge と接続し、ESP-NOW で Hapbeat 本体と通信する。クラウド接続は不要であり、インターネット環境がなくても動作する前提で設計する。

## Pack / Event ID / targetTime / Bridge のうち重要な概念

- **Bridge（入力元として）が最重要** — 送信機ファームのすべての入力は Bridge から来る。Bridge との内部インターフェースが送信機ファームの設計を規定する
- **Event ID は中継するが解決しない** — Event ID はパケットに含めて本体へ転送するが、その意味の解釈（どの触覚パターンを再生するか）は本体ファーム側の責務
- **Pack / targetTime は直接扱わない** — Pack の読み込みや targetTime に基づくスケジューリングは Bridge や本体ファームの責務であり、送信機ファームでは関与しない

## 指示書

- `instructions/` — 他セッションからの未実行の指示書
- `instructions/completed/` — 完了済みの指示書
- セッション開始時に `instructions/` を確認し、該当する指示書があれば適用する
